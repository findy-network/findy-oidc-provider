{
  "name": "findy-oidc-provider",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "deploy": "rm lambda.zip || true && npm run package && cd infra && npm install && cdk deploy -y",
    "package": "rm -rf node_modules && npm install --only=production && zip -r lambda.zip node_modules src",
    "start": "node src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "Apache-2.0",
  "dependencies": {
    "@vendia/serverless-express": "^4.5.3",
    "express": "^4.17.2",
    "helmet": "^5.0.2",
    "lodash": "^4.17.21",
    "oidc-provider": "^7.10.6"
  }
}
